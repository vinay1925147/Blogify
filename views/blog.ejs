<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog page</title>
    
   
</head>
<body>
       <%#- include('./subTemplates/nav') %>
        <h1> About :</h1>
    <div class="container mt-4" >
        <h2 style="font-size: 30px;"> <%=blog.title  %></h2>   
        <img src="<%= blog.coverImageURL %>" alt="vinau">
        <p style="width: auto; font-size:25px; font-weight: 600;"> <%= blog.body  %></p>
    </div>
     
     <div class="container mt-4">
     <img src="<%= blog.createdBy.profileImageURL %>" width="50px" alt="profile" >
      <%= blog.createdBy.fullName %>
    </div>


    <div class="container mt-3">
      <h1>Comments (<%= comments.length %>)  </h1>
      <% if (locals.user) { %>
      <form action="/blog/comment/<%= blog._id %>" method="post">
        <div class="mb-3">
          
          <input
            type="text"
            name="content"
            class="form-control"
            placeholder="Enter Your Comment"
            />
          <button class="btn btn-sm btn-primary" type="submit">Add</button>
        </div>
      </form>

   <% } %>
    <div class="mt-3">
        <% comments.forEach(comment => { %>
        <div>
          <img src="<%= comment.createdBy.profileImageURL %>" width="50px" />
          <%= comment.createdBy.email %>
          <pre><%= comment.content %></pre>
        </div>
        <% }) %>
      </div>
    </div>

  <%- include('./subTemplates/script') %>
</body>
</html>