<!DOCTYPE html>
<html lang="en">
<head>
   <%- include('subTemplates/head') %>
   <title>Home page</title>
    <style>
       .container{
            display:flex;
            align-items: flex-start;
            justify-content: space-evenly;
            width: 1000px;
          }
    </style>
</head>
<body>
  <%- include('subTemplates/nav') %>
    <%#= JSON.stringify(user) %>
 <div class="container m-4 flex" >
    <% for(let blog of blogs) {  %>
<div class="container text-center">
  <div class="row">
    <div class="col">
         <div class="card m-2" style="width: 18rem; " >
       <img src="<%= blog.coverImageURL %>"     alt="vinay" 
     style="width: fit-content; height: 200px;">
          <div class="card-body">
            <h5 class="card-title"><%= blog.title %></h5>
            <a href="/blog/<%= blog.id %>" class="btn btn-primary">View</a>
          </div>
        </div> 
    </div>
  </div>
</div>
     <% } %>
 </div>
  <%- include('subTemplates/script') %>
</body>
</html>


       